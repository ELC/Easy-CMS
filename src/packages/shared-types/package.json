{
  "name": "shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint . && prettier --check .",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('shared-types has no browser behavior tests')\"",
    "test:record": "node -e \"console.log('shared-types has no HAR fixtures')\"",
    "build": "tsc --noEmit",
    "orval": "node -e \"console.log('shared-types has no Orval output')\""
  },
  "devDependencies": {
    "typescript": "*"
  }
}
